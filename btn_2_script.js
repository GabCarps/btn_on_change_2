const showImageBtn = document.getElementById('show_image');
const image = document.getElementById('image');

showImageBtn.addEventListener('click', function() {
  image.style.display = 'block';
});